clc;
clear all;
close all;
x=input('seq')
N=length(x);
xk=fft(x,N);
n=0:N-1
subplot(2,1,1);
stem(n,abs(xk));
xlabel('time')
ylabel('amp')
title('magnitude spectrum')
subplot(2,1,2);
stem(n,angle(xk));
xlabel('time')
ylabel('amp')
title('phase spectrum')